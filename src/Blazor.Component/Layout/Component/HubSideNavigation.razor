<MudNavMenu Color="Color.Primary" Bordered=true Dense="true">
    <MudNavLink
        Disabled="false"
        Match="NavLinkMatch.All"
        Href="@Environment.BaseAddress"
        Icon="@Icons.Material.Filled.Home" >
        Home
    </MudNavLink>

    <MudNavLink
        ActiveClass=""
        Match="NavLinkMatch.All"
        Icon="@Icons.Material.Filled.School"
        OnClick="@(() => ScrollManager.ScrollIntoViewAsync("#" + BlazorPage.Playground.Section, ScrollBehavior.Smooth))">
        Playground
    </MudNavLink>

    <MudText
        Class="ml-4 my-3"
        Typo="Typo.subtitle2"
        Color="Color.Inherit" >
        API Documentation
    </MudText>

    <MudNavLink
        ActiveClass=""
        Match="NavLinkMatch.All"
        Icon="@CustomIcon.SwaggerUi"
        OnClick="@(() => ScrollManager.ScrollIntoViewAsync("#" + BlazorPage.Documentation.Swagger, ScrollBehavior.Smooth))">
        Swagger
    </MudNavLink>

    <MudNavLink
        ActiveClass=""
        Match="NavLinkMatch.All"
        Icon="@CustomIcon.Scalar"
        OnClick="@(() => ScrollManager.ScrollIntoViewAsync("#" + BlazorPage.Documentation.Scalar, ScrollBehavior.Smooth))">
        Scalar
    </MudNavLink>

    <MudNavLink
        ActiveClass=""
        Match="NavLinkMatch.All"
        Icon="@CustomIcon.GraphQl"
        OnClick="@(() => ScrollManager.ScrollIntoViewAsync("#" + BlazorPage.Documentation.GraphQl, ScrollBehavior.Smooth))">
        GraphQL
    </MudNavLink>

    <MudText
        Class="ml-4 my-3"
        Typo="Typo.subtitle2"
        Color="Color.Inherit" >
        Observability
    </MudText>

    <MudNavLink
        ActiveClass=""
        Match="NavLinkMatch.All"
        Icon="@CustomIcon.OpenTelemetry"
        OnClick="@(() => ScrollManager.ScrollIntoViewAsync("#" + BlazorPage.Observability.OpenTelemetry, ScrollBehavior.Smooth))">
        OpenTelemetry
    </MudNavLink>

    <MudNavLink
        ActiveClass=""
        Match="NavLinkMatch.All"
        Icon="@CustomIcon.NetCore"
        OnClick="@(() => ScrollManager.ScrollIntoViewAsync("#" + BlazorPage.Observability.HealthCheck, ScrollBehavior.Smooth))">
        Health Check
    </MudNavLink>

    <MudText
        Class="mx-4 my-3"
        Typo="Typo.subtitle2"
        Color="Color.Inherit" >
        Security
    </MudText>

    <MudNavLink
        Disabled="@(HubFeatureManagerContext.IsFeatureEnabled(HubFeatureName.HubWebAuthentication) is false)"
        ActiveClass=""
        Match="NavLinkMatch.All"
        Icon="@Icons.Material.Filled.Fingerprint"
        OnClick="@(() => ScrollManager.ScrollIntoViewAsync("#" + BlazorPage.Security.WebAuthn, ScrollBehavior.Smooth))">
        WebAuthn
    </MudNavLink>

    <MudNavLink
        Disabled="@(HubFeatureManagerContext.IsFeatureEnabled(HubFeatureName.HubMfa) is false)"
        ActiveClass=""
        Match="NavLinkMatch.All"
        Icon="@Icons.Material.Filled.QrCodeScanner"
        OnClick="@(() => ScrollManager.ScrollIntoViewAsync("#" + BlazorPage.Security.Mfa, ScrollBehavior.Smooth))">
        MFA
    </MudNavLink>

    <MudText
        Class="mx-4 my-3"
        Typo="Typo.subtitle2"
        Color="Color.Inherit" >
        Communication
    </MudText>

    <MudNavLink
        Disabled="@(HubFeatureManagerContext.IsFeatureEnabled(HubFeatureName.HubSignalR) is false)"
        ActiveClass=""
        Match="NavLinkMatch.All"
        Icon="@CustomIcon.SignalR"
        OnClick="@(() => ScrollManager.ScrollIntoViewAsync("#" + BlazorPage.Communication.SignalR, ScrollBehavior.Smooth))">
        SignalR
    </MudNavLink>

    <MudNavLink
        Disabled="@(HubFeatureManagerContext.IsFeatureEnabled(HubFeatureName.HubGrpc) is false)"
        ActiveClass=""
        Match="NavLinkMatch.All"
        Icon="@CustomIcon.Grpc"
        OnClick="@(() => ScrollManager.ScrollIntoViewAsync("#" + BlazorPage.Communication.Grpc, ScrollBehavior.Smooth))">
        Grpc
    </MudNavLink>
    
</MudNavMenu>