<MudNavMenu Color="Color.Primary" Bordered=true Dense="true">
    <MudNavLink
        Disabled="false"
        Match="NavLinkMatch.All"
        Href="@Environment.BaseAddress"
        Icon="@Icons.Material.Filled.Home" >
        Home
    </MudNavLink>

    <MudNavGroup
        HideExpandIcon="true"
        Title="Playground"
        Icon="@Icons.Material.Filled.School" >

        <MudNavLink
            Disabled="true"
            Match="NavLinkMatch.All"
            Icon="@Icons.Material.Filled.Watch"
            Href="@BlazorPage.Playground.Counter" >
            Counter
        </MudNavLink>
        
        <MudNavLink
            Disabled="false"
            Match="NavLinkMatch.All"
            Icon="@Icons.Material.Filled.CleanHands"
            Href="@BlazorPage.Playground.Chore" >
            Chore
        </MudNavLink>

        <MudNavLink
            Disabled="true"
            Match="NavLinkMatch.All"
            Icon="@Icons.Material.Filled.DataUsage"
            Href="@BlazorPage.Playground.FetchData" >
            Fetch data
        </MudNavLink>

    </MudNavGroup>

    <MudText
        Class="ml-4 my-3"
        Typo="Typo.subtitle2"
        Color="Color.Inherit" >
        API Documentation
    </MudText>

    <MudNavLink
        Disabled="@(HubFeatureManagerContext.IsFeatureEnabled(HubFeatureName.HubSwagger) is false)"
        ActiveClass=""
        Match="NavLinkMatch.All"
        Icon="@CustomIcon.SwaggerUi"
        Href="@("#" + BlazorPage.Documentation.Swagger)" >
        Swagger
    </MudNavLink>

    <MudNavLink
        Disabled="@(HubFeatureManagerContext.IsFeatureEnabled(HubFeatureName.HubScalar) is false)"
        ActiveClass=""
        Match="NavLinkMatch.All"
        Icon="@CustomIcon.Scalar"
        Href="@("#" + BlazorPage.Documentation.Scalar)">
        Scalar
    </MudNavLink>

    <MudNavLink
        Disabled="@(HubFeatureManagerContext.IsFeatureEnabled(HubFeatureName.HubGraphQl) is false)"
        ActiveClass=""
        Match="NavLinkMatch.All"
        Icon="@CustomIcon.GraphQl"
        Href="@("#" + BlazorPage.Documentation.GraphQl)">
        GraphQL
    </MudNavLink>

    <MudText
        Class="ml-4 my-3"
        Typo="Typo.subtitle2"
        Color="Color.Inherit" >
        Observability
    </MudText>

    <MudNavLink
        Disabled="@(HubFeatureManagerContext.IsFeatureEnabled(HubFeatureName.HubOpenTelemetry) is false)"
        ActiveClass=""
        Match="NavLinkMatch.All"
        Icon="@CustomIcon.OpenTelemetry"
        Href="@("#" + BlazorPage.Observability.OpenTelemetry)" >
        OpenTelemetry
    </MudNavLink>

    <MudNavLink
        Disabled="@(HubFeatureManagerContext.IsFeatureEnabled(HubFeatureName.HubHealthCheck) is false)"
        ActiveClass=""
        Match="NavLinkMatch.All"
        Icon="@CustomIcon.NetCore"
        Href="@("#" + BlazorPage.Observability.HealthCheck)" >
        Health Check
    </MudNavLink>

    <MudText
        Class="mx-4 my-3"
        Typo="Typo.subtitle2"
        Color="Color.Inherit" >
        Security
    </MudText>

    <MudNavLink
        Disabled="@(HubFeatureManagerContext.IsFeatureEnabled(HubFeatureName.HubWebAuthentication) is false)"
        ActiveClass=""
        Match="NavLinkMatch.All"
        Icon="@Icons.Material.Filled.Fingerprint"
        Href="@("#" + BlazorPage.Security.WebAuthn)" >
        WebAuthn
    </MudNavLink>

    <MudNavLink
        Disabled="@(HubFeatureManagerContext.IsFeatureEnabled(HubFeatureName.HubMfa) is false)"
        ActiveClass=""
        Match="NavLinkMatch.All"
        Icon="@Icons.Material.Filled.QrCodeScanner"
        Href="@("#" + BlazorPage.Security.Mfa)" >
        MFA
    </MudNavLink>

    <MudText
        Class="mx-4 my-3"
        Typo="Typo.subtitle2"
        Color="Color.Inherit" >
        Communication
    </MudText>

    <MudNavLink
        Disabled="@(HubFeatureManagerContext.IsFeatureEnabled(HubFeatureName.HubSignalR) is false)"
        ActiveClass=""
        Match="NavLinkMatch.All"
        Icon="@CustomIcon.SignalR"
        Href="@BlazorPage.Communication.SignalR" >
        SignalR
    </MudNavLink>

    <MudNavLink
        Disabled="@(HubFeatureManagerContext.IsFeatureEnabled(HubFeatureName.HubGrpc) is false)"
        ActiveClass=""
        Match="NavLinkMatch.All"
        Icon="@CustomIcon.Grpc"
        Href="@BlazorPage.Communication.Grpc" >
        Grpc
    </MudNavLink>
    
</MudNavMenu>